/**
 * 販售據點頁面 (Server Component)
 *
 * 客戶端互動邏輯由 LocationsPageClient 處理（列表/地圖切換、GPS 搜尋）。
 */

import { LocationsPageClient } from './LocationsPageClient'

export default function LocationsPage() {
  return <LocationsPageClient />
}
